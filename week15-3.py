class Solution:
    def equalSubstring(self, s: str, t: str, maxCost: int) -> int:
        N = len(s)
        for i in range(N):
            print( s[i],t[i])# 先用 Python 把字串的迴圈寫出來

        ans = 0
        return ans
